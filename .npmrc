# NODE_ENV=production is set globally on this machine, which makes npm skip
# devDependencies. MarkNote keeps build/runtime libs in devDependencies
# (Vite bundles them), so dev deps must always be installed.
include=dev
audit=false
fund=false
